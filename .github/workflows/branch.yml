name: Branch Workflow

on:
  push:
    branches-ignore:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Cache pip
        uses: actions/cache@v2
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Install Terraform
        run: |
            mkdir .tmp
            curl -o .tmp/tf.zip https://releases.hashicorp.com/terraform/<span class="math-inline">\{\{ inputs\.terraform\_version \}\}/terraform\_</span>{{ inputs.terraform_version }}_linux_amd64.zip
            cd .tmp
            unzip tf.zip
            sudo mv terraform /usr/local/bin/
            cd ..
            rm -rf .tmp
            echo "Installed version $(terraform -version) of Terraform"
        shell: bash
